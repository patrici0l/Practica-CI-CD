apiVersion: v1
kind: Service
metadata:
  name: inventario-service-bg
spec:
  type: NodePort
  selector:
    app: inventario-app
    version: v2   #cambiamos para que apunte a la version verde y corte de trafico 
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30081
