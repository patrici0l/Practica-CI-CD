apiVersion: v1
kind: Service
metadata:
  name: inventario-service-bg
spec:
  type: NodePort
  selector:
    app: inventario-app
    version: v1
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30081
